services:
  - type: web
    name: task-management
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: task-db
          property: connectionString
      - key: PORT
        value: 8000

databases:
  - name: task-db
    plan: free
